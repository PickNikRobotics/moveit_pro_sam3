# moveit_pro_sam3

ROS package containing SAM3 ONNX models for image segmentation.

## Usage

Submodule this repo into a user_workspace to make the models available in the `moveit_pro_sam3` package for use by MoveIt Pro Behaviors.
